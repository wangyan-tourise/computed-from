import Vue from 'vue';
import Vuex from 'vuex';

Vue.use(Vuex);

export default new Vuex.Store({
  state: {
    
	},
	mutations: {
    // sysauths(state, val) {
      // val.splice(0, 0, state.breadcrumbbase);
      // state.sysauths = val;
		// },
	},
	actions: {
  },
});